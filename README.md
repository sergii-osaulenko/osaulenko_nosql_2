# Семантичний пошук за науковими статтями

Цей репозиторій містить повний пайплайн для семантичного, лексичного (BM25) та гібридного пошуку (через RRF) за анотаціями 10 000 наукових статей з датасету **arXiv** (підмножина 2007 року). У цьому документі наведено теоретичні обґрунтування вибору інструментів, аналіз метрик схожості, порівняння стратегій чанкінгу та результати роботи гібридного пошуку.

---

### 1. Налаштування .gitignore

У корені репозиторію налаштовано файл `.gitignore`, який виключає потрапляння конфіденційних даних та важких бінарних файлів у систему контролю версій:

```git
.env
data/
embeddings/
```

---

### 2. Теоретичні відповіді та обґрунтування

##### Частина 1. Вибір інструментів та ембеддингів
1. **Порівняння Pinecone, Qdrant та Chroma:**
   * **Pinecone:** Повністю керована хмарна векторна база даних (SaaS) із пропрієтарною ліцензією. Забезпечує високу масштабованість, автоматичне шардування та мінімальний час налаштування інфраструктури, проте має обмеження у безкоштовному тірі (1 індекс, до 100 000 векторів) та є платною для продакшен-рішень. Оптимальний вибір для комерційних систем із великим навантаженням, де в пріоритеті швидкість розгортання та відсутність витрат на адміністрування.
   * **Qdrant:** Високопродуктивна векторна база даних, написана на Rust, з відкритим вихідним кодом (ліцензія Apache 2.0). Може розгортатися як локально, так і в хмарі, підтримує розширену фільтрацію за корисним навантаженням (payload). Ідеально підходить для високонавантажених кастомних інфраструктур із гнучкими вимогами до безпеки даних.
   * **Chroma:** Вбудована (embedded) векторна база даних з відкритим кодом, яка найчастіше працює локально або в оперативній пам'яті. Найкращий вибір для швидкого прототипування, досліджень та локальних розробок, проте вона поступається продуктивністю при роботі з мільйонними датасетами.
2. **Чому обрано `allenai/specter2_base` замість `all-MiniLM-L6-v2`?**
   * Згідно з офіційною карткою моделі на HuggingFace, **`allenai/specter2_base`** — це спеціалізована модель на рівні документів для наукової літератури (*"document-level embedding model for scientific literature"*), яка навчалася на графі цитувань та академічних текстах. Універсальна модель `all-MiniLM-L6-v2` орієнтована на короткі тексти загальної тематики і значно гірше вловлює специфічну наукову термінологію, контекст та семантичні зв'язки між публікаціями.
3. **Рекомендована метрика схожості:**
   * У картці моделі SPECTER2 для порівняння векторів рекомендовано використовувати **косинусну схожість (cosine similarity)** або скалярний добуток для попередньо нормалізованих векторів. Це критично важливо при створенні індексу, оскільки геометрія простору ознак цієї моделі оптимізована саме під кутову відстань між семантичними напрямками, а не під абсолютні евклідові відстані.
4. **Еквівалентність Cosine Similarity та Dot Product:**
   * Косинусна схожість між двома векторами $\mathbf{u}$ та $\mathbf{v}$ обчислюється за формулою:
     $$\text{cosine}(\mathbf{u}, \mathbf{v}) = \frac{\mathbf{u} \cdot \mathbf{v}}{\|\mathbf{u}\| \|\mathbf{v}\|}$$
   * Коли ми використовуємо нормалізовані ембеддинги одиничної довжини (`normalize_embeddings=True`), норми векторів дорівнюють одиниці ($\|\mathbf{u}\| = 1$ та $\|\mathbf{v}\| = 1$). У результаті знаменник стає рівним $1$, і формула спрощується до:
     $$\text{cosine}(\mathbf{u}, \mathbf{v}) = \mathbf{u} \cdot \mathbf{v}$$
   * Таким чином, для нормалізованих векторів косинусна схожість математично тотожна скалярному добутку, що дозволяє виконувати пошук швидше без додаткових операцій ділення.

##### Частина 3. Пошукові запити та аналіз метрик
1. **Чи збігаються топ-5 для Cosine та Dot Product?**
   * **Так, повністю збігаються.** Наш практичний експеримент у скрипті `04_search.py` довів це емпірично: обидва методи повернули ідентичний масив індексів `[378, 3350, 4115, 610, 3181]`. Оскільки під час кодування використано параметр `normalize_embeddings=True`, вектори мають одиничну норму, і результати ранжування є тотожними.
2. **Чи відрізняються результати для L2 Distance?**
   * У нашому експерименті на нормалізованих векторах результати **L2 Distance** також дали тотожний порядок індексів `[378, 3350, 4115, 610, 3181]`. Це пояснюється тим, що квадрат евклідової відстані на одиничній сфері монотонно залежить від скалярного добутку: $\|\mathbf{u} - \mathbf{v}\|^2 = 2 - 2(\mathbf{u} \cdot \mathbf{v})$. Однак на інших (не нормалізованих) датасетах або при похибках округляння чисел із плаваючою комою (float precision) порядок ранжування може відрізнятися.
3. **Що сталося б, якби ембеддинги не були нормалізовані?**
   * Без нормалізації скалярний добуток залежав би не лише від кута між векторами (семантичної близькості), а й від їхньої довжини (магнітуди). Довші або насиченіші термінами анотації отримували б штучно завищені оцінки схожості, і результати **Dot Product** кардинально відрізнялися б від **Cosine Similarity**.

##### Частина 4. Стратегії розбиття тексту
1. **Яка стратегія дає більш осмислені чанки?**
   * **Семантичний чанкінг (Semantic chunking)** дає значно якісніші результати. Він формує фрагменти, орієнтуючись на межі речень, що зберігає завершену думку автора. **Fixed-size chunking** (фіксований розмір) механічно ділить текст за кількістю слів, через що контекст часто розривається.
2. **Чи є випадки розрізаних речень і як це впливає на ембеддинги?**
   * У стратегії з фіксованим розміром речення регулярно розрізаються посередині. Це погіршує якість ембеддингів: модель змушена кодувати неповні синтаксичні конструкції (наприклад, підмет без присудка або відірвані підрядні речення), що спотворює семантичний вектор чанка.
3. **Вплив розміру Overlap (перекриття):**
   * Збільшення перекриття між чанками **підвищує покриття тексту** і запобігає втраті сенсу на стиках фрагментів.
   * Водночас це **збільшує загальну кількість чанків**, що призводить до більших витрат пам'яті в індексі Pinecone та уповільнює індексацію.

##### Частина 6. Підсумковий аналіз та висновки
1. **Семантичний пошук vs BM25: загальне правило:**
   * **BM25** є неперевершеним для запитів, що містять точні власні назви, прізвища авторів, унікальні технічні абревіатури або номенклатуру (наприклад, *"Yann LeCun"* або *"BERT fine-tuning"*).
   * **Семантичний пошук** перевершує класичний підхід на концептуальних, описових запитах природною мовою, де важливий зміст, а не конкретні лексеми (наприклад, *"making computers understand human emotions from text"*).
   * **Правило:** *Використовуйте BM25 для точного лексичного пошуку та фільтрації сутностей, семантичний — для пошуку за змістом і концепціями, а гібридний (RRF) — як універсальне рішення для продакшену.*
2. **Вплив розміру чанка на якість пошуку:**
   * **Занадто маленькі чанки (10–15 слів):** Втрачають глобальний контекст. Модель генерує вектор для окремої фрази, яка у відриві від статті може означати що завгодно.
   * **Занадто великі чанки (500+ слів):** Концентрований зміст «розмивається». Вектор стає усередненим значенням багатьох тем, що знижує точність пошуку конкретних деталей.
   * **Оптимальний розмір:** Для наукових анотацій найкраще підходять чанки розміром **100–250 слів** із перекриттям у **15–20%**, що дозволяє зберегти закінчені думки без семантичного шуму.
3. **Математичне обґрунтування невідповідної метрики (L2 для нормалізованих векторів):**
   * Якщо створити індекс Pinecone із метрикою `euclidean` ($L_2$), але завантажувати нормалізовані вектори ($\|\mathbf{u}\| = 1$, $\|\mathbf{v}\| = 1$), квадрат евклідової відстані пов'язаний із косинусом кута $\theta$ між ними так:
     $$\|\mathbf{u} - \mathbf{v}\|^2 = (\mathbf{u} - \mathbf{v}) \cdot (\mathbf{u} - \mathbf{v}) = \|\mathbf{u}\|^2 + \|\mathbf{v}\|^2 - 2(\mathbf{u} \cdot \mathbf{v}) = 1 + 1 - 2\cos(\theta) = 2(1 - \cos(\theta))$$
   * Оскільки функція $f(x) = 2(1 - x)$ є строго спадною, **мінімізація евклідової відстані математично еквівалентна максимізації косинусної схожості**. Порядок ранжування документів не зміниться, однак значення оцінок (scores), які поверне Pinecone, будуть лежати в діапазоні $[0, 2]$, де $0$ означатиме повний збіг, що може зламати логіку подальшої фільтрації за порогом схожості (thresholding).
4. **Обмеження Pinecone Starter та масштабування до 10 млн статей:**
   * **Обмеження Starter-тіру:** Безкоштовний тариф дозволяє створити лише 1 індекс і зберігати до 100 000 векторів. При спробі завантажити чанки з великого датасету ліміт вичерпується миттєво.
   * **Рішення для 10 мільйонів статей:**
     1. **Перехід на Self-Hosted / Open-Source рішення:** Використання **Qdrant** або **Milvus**, розгорнутих у Kubernetes-кластері на власних або орендованих серверах.
     2. **Оптимізація індексації:** Використання апаратного прискорення та індексів на базі **HNSW** (Hierarchical Navigable Small World) для сублінійного часу пошуку.
     3. **Квантування векторів (Vector Quantization):** Застосування Scalar Quantization (SQ8) або Product Quantization (PQ), що дозволяє зменшити споживання оперативної пам'яті в 4–8 разів із мінімальною втратою точності.
     4. **Дворівневий пошук (Retrieve & Re-rank):** Попередній швидкий відбір топ-1000 кандидатів через BM25/грубий векторний індекс із подальшим переранжуванням за допомогою важкої cross-encoder моделі.

---

### 3. Порівняльна таблиця методів пошуку

> **Примітка щодо датасету:** Оскільки в роботі використовувалася підмножина архівних наукових статей 2007 року (`arXiv-0704`), сучасні терміни (наприклад, нейромережі трансформерів BERT або роботи Янна ЛеКуна з CNN, популярні в пізніші роки) відсутні в прямому вигляді. Тому пошук виявляє історичні концепції, які збігаються за термінологією (наприклад, Fine-Tuning у фізиці елементарних частинок або перцептрони/градієнтні мережі в ранньому ML).

| Запит | Метод BM25 (Топ-1) | Векторний пошук (Топ-1) | Гібридний пошук із RRF (Топ-1) | Аналіз результатів |
| :--- | :--- | :--- | :--- | :--- |
| **"BERT fine-tuning"** | *The NMSSM Solution to the Fine-Tuning Problem...* (Score: 11.50) | *Misere quotients for impartial games: Supplementary material* (Score: 0.8645) | *The NMSSM Solution to the Fine-Tuning Problem...* (RRF Score: 0.0164) | **BM25 та RRF** відпрацювали краще за точним ключовим словом *"Fine-Tuning"*, знайшовши фундаментальні статті з проблеми тонкого налаштування, тоді як чистий векторний пошук видав шум. |
| **"Yann LeCun convolutional networks"** | *On Punctured Pragmatic Space-Time Codes in Block Fading Channel* (Score: 13.48) | *Multilayer Perceptron with Functional Inputs: an Inverse Regression Approach* (Score: 0.8479) | *Optimization in Gradient Networks* (RRF Score: 0.0303) | **Гібридний пошук (RRF)** виявився найкращим: він об'єднав семантику нейромереж із терміном *Networks* і поставив на 1-ше місце статтю про *Gradient Networks* (найближче до CNN). |
| **"making computers understand human emotions from text"** | *An Automated Evaluation Metric for Chinese Text Entry* (Score: 18.27) | *Opinion Dynamics and Sociophysics* (Score: 0.8287) | *On the Development of Text Input Method - Lessons Learned* (RRF Score: 0.0323) | **Векторний пошук** найкраще вловив концепцію соціальної взаємодії та аналізу думок (*Opinion Dynamics and Sociophysics*), тоді як BM25 зреагував лише на слова *Text/Computers*. |

---

### 4. Вивід деталей виконання скриптів

##### 01_prepare_data.py

```log
Читаємо датасет: 10000it [00:00, 83776.84it/s]

Завантажено статей:10000

Розподіл за категоріями (топ-10):
category
astro-ph              1838
hep-th                 680
hep-ph                 671
quant-ph               564
gr-qc                  350
cond-mat.mes-hall      307
cond-mat.str-el        292
cond-mat.mtrl-sci      291
cond-mat.stat-mech     271
math.AG                209
Name: count, dtype: int64

Розподіл за роками:
year
2007    10000
Name: count, dtype: int64

Приклад запису:
{'id': '0704.0001', 'title': 'Calculation of prompt diphoton production cross sections at Tevatron and\n  LHC energies', 'abstract': 'A fully differential calculation in perturbative quantum chromodynamics is\npresented for the production of massive photon pairs at hadron colliders. All\nnext-to-leading order perturbative contributions from quark-antiquark,\ngluon-(anti)quark, and gluon-gluon subprocesses are included, as well as\nall-orders resummation of initial-state gluon radiation valid at\nnext-to-next-to-leading logarithmic accuracy. The region of phase space is\nspecified in which the calculation is most reliable. Good agreement is\ndemonstrated with data from the Fermilab Tevatron, and predictions are made for\nmore detailed tests with CDF and DO data. Predictions are shown for\ndistributions of diphoton pairs produced at the energy of the Large Hadron\nCollider (LHC). Distributions of the diphoton pairs from the decay of a Higgs\nboson are contrasted with those produced from QCD processes at the LHC, showing\nthat enhanced sensitivity to the signal can be obtained with judicious\nselection of events.', 'authors': 'BalázsC., BergerE. L., NadolskyP. M., YuanC. -P.', 'year': 2007, 'category': 'hep-ph'}

Збережено вdata/arxiv_subset.parquet
```

##### 02_embed.py

```log
Loading weights: 100%|█████████████████████████████████████████████████████████| 199/199 [00:00<00:00, 25581.30it/s]
Генеруємо ембеддинги для 10000 текстів...                                                  | 0.00/125 [00:00<?, ?B/s]
Batches: 100%|████████████████████████████████████████████████████████████████████| 157/157 [58:11<00:00, 22.24s/it]

Загальна кількість оброблених текстів: 10000
Розмірність ембеддингів: 768
Норма першого ембеддингу: 1.0000
Збережено в embeddings/embeddings.npy
```

##### 03_load_to_pinecone.py

```log
Завантаження даних у Pinecone індекс 'arxiv-papers'...
Завантаження батчів: 100%|██████████████████████████████████████████████████████████| 50/50 [01:24<00:00,  1.68s/it]

Загальна кількість векторів в індексі: 10000
```

##### 04_search.py

```log
Loading weights: 100%|█████████████████████████████████████████████████████████| 199/199 [00:00<00:00, 33597.65it/s]
=== Чистий семантичний пошук за запитом: 'teaching machines to recognize objects in pictures' ===
- [cond-mat.soft] (2007.0) Capturing knots in polymers (Score: 0.8288)
  Абстракт: This paper visualizes a knot reduction algorithm...

- [physics.ins-det] (2007.0) Symbolic sensors : one solution to the numerical-symbolic interface (Score: 0.8263)
  Абстракт: This paper introduces the concept of symbolic sensor as an extension of the
smart sensor one. Then, the links between the physical world and the symbolic
one are introduced. The creation of symbols is...

- [math.HO] (2007.0) The Mathematics (Score: 0.8256)
  Абстракт: This is an essay that considering the knowledge structure and language of a
different nature, attempts to build on an explanation of the object of study
and characteristics of the mathematical science...

- [physics.comp-ph] (2007.0) Modeling the field of laser welding melt pool by RBFNN (Score: 0.8170)
  Абстракт: Efficient control of a laser welding process requires the reliable prediction
of process behavior. A statistical method of field modeling, based on
normalized RBFNN, can be successfully used to predic...

- [quant-ph] (2007.0) Why should anyone care about computing with anyons? (Score: 0.8146)
  Абстракт: In this article we present a pedagogical introduction of the main ideas and
recent advances in the area of topological quantum computation. We give an
overview of the concept of anyons and their exoti...

=== Пошук з фільтрацією (Приклад A: RL за останні 5 років, cs.LG) ===

=== Порівняння метрик на локальних ембеддингах ===
Топ-5 Cosine Similarity індекси: [ 378 3350 4115  610 3181]
Топ-5 Dot Product індекси: [ 378 3350 4115  610 3181]
Топ-5 L2 Distance індекси: [ 378 3350 4115  610 3181]
```

##### 05_chunking.py

```log
Loading weights: 100%|█████████████████████████████████████████████████████████| 199/199 [00:00<00:00, 30023.97it/s]
Чанки успішно завантажено в індекси Pinecone.
```